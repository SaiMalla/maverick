<%= form_with(model: ticket_sub_type, local: true) do |form| %>
  <% if ticket_sub_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_sub_type.errors.count, "error") %> prohibited this ticket_sub_type from being saved:</h2>

      <ul>
      <% ticket_sub_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ticketsubtype %>
    <%= form.text_field :ticketsubtype %>
  </div>

  <div class="field">
    <%= form.label :createdby %>
    <%= form.number_field :createdby %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="field">
    <%= form.label :ticket_type_id %>
    <%= form.text_field :ticket_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
