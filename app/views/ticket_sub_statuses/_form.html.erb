<%= form_with(model: ticket_sub_status, local: true) do |form| %>
  <% if ticket_sub_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_sub_status.errors.count, "error") %> prohibited this ticket_sub_status from being saved:</h2>

      <ul>
      <% ticket_sub_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ticketsubstatus %>
    <%= form.text_field :ticketsubstatus %>
  </div>

  <div class="field">
    <%= form.label :creatredby %>
    <%= form.number_field :creatredby %>
  </div>

  <div class="field">
    <%= form.label :ticket_status_id %>
    <%= form.text_field :ticket_status_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
